#include <stdio.h>
#include <math.h>
int main() {
    int a = 5;
    int b = 8;
    int c; 
    c = sqrt(a * a + b * b);
    printf("The hypotenuse of the triangle is %d", c);
    return 0;
}